Clone into the repo:
`git clone git@github.com:kalikokalikova/moodtracker.git`

To run the backend:
`cd server`
`uv run uvicorn main:app --reload`

To run the frontend:
`cd client`
`npm install`
`npm run dev`
